requests>=2.31
beautifulsoup4>=4.12
openpyxl>=3.1
pypdf>=4.0

# Optional: only needed if you want JavaScript-rendered pages fetched via a
# real browser (config.PLAYWRIGHT_FALLBACK_ENABLED). If you skip this,
# the scraper still works for every site that doesn't require JS rendering,
# it just logs a warning and uses the static HTML instead.
playwright>=1.42
